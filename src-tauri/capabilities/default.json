{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for Drift windows.",
  "windows": ["main", "session-*"],
  "permissions": ["core:default"]
}
